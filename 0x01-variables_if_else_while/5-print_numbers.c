#include <stdio.h>
/**
 * main - Beginning of code execution
 * Return: always 0(success)
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);
	putchar('\n');
	return (0);
}
